package figure;

public class Square extends Figure{

    public Square(int width, int length)
    {
        setWidth(width);
        setLength(length);
        setRadius(0);
        setHeight(0);
    }
}
