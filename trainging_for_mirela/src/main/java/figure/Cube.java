package figure;

public class Cube extends Figure{
    public Cube(int height, int width, int length)
    {
        setWidth(width);
        setLength(length);
        setHeight(height);
        setRadius(0);
    }
}
